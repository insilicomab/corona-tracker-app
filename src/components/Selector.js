import countriesJson from "../countries.json";

const Selector =()=> {
    return(
        <div>
            <select>
                <option></option>
            </select>
        </div>
    );
};

export default Selector;